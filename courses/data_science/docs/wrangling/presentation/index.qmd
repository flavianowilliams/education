---
title: "Manipulação de dados com o pacote tidyverse"
---

## Quarto

Quarto enables you to weave together content and executable code into a finished presentation. To learn more about Quarto presentations see <https://quarto.org/docs/presentations/>.

## Bullets

When you click the **Render** button a document will be generated that includes:

- Content authored with markdown
- Output from executable code

## Operador pipe (|> ou %>%)

:::: {.columns}

::: {.column width="50%"}
### Definindo função Fibonacci

```{r, echo=TRUE}
fibonacci <- function(dataframe, x) {
  vetor <- dataframe[, x]
  soma <- rep(0, length(vetor))
  for (j in 1:length(vetor)) {
    a <- 0
    b <- 1
    num <- a+b
    soma[j] <- 0
    for (i in 1:vetor[j]) {
      num <- a+b
      a <- b
      b <- num
      soma[j] <- soma[j]+a
    }
  }
  return(soma)
}
```
:::

::: {.column width="50%"}
### Aplicando operador pipe

```{r, echo=TRUE}
# dataframe
dados <- data.frame(a = seq(1, 5))
str(dados)
# aplicando a função fibonacci
fibonacci(dados, "a")
# aplicando a função com o operador pipe
dados |> 
  fibonacci("a")
```
:::

::::